subdomain: audiocpp
# External service configuration
externalService:
  ip: 192.168.10.197
  port: 8081

# Defaults for local linting/testing (overridden by root apps values)
globals:
  domain: olsen.cloud
  istio:
    gateway: shared/private
