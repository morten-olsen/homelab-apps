image:
  repository: bbernhard/signal-cli-rest-api
  tag: "0.101@sha256:2cf09d66a86fdd210e42ee91662216364acc27da1163a8c44167a77ea3a6559a"
  pullPolicy: IfNotPresent

subdomain: apprise

# DNS configuration
dns:
  enabled: true
  type: A
  dnsClassRef:
    name: private-dns

command:
  - /usr/bin/signal-cli
args:
  - --config=/home/.local/share/signal-cli
  - daemon
  - --http=0.0.0.0:8080

deployment:
  strategy: Recreate
  replicas: 1

container:
  port: 8080
  healthProbe:
    type: tcpSocket

service:
  port: 80

volumes:
  - name: data
    mountPath: /home/.local/share/signal-cli
    persistentVolumeClaim: data

persistentVolumeClaims:
  - name: data
    size: 1Gi
    storageClassName: local-path
    backup: true

probe:
  enabled: false

virtualService:
  enabled: false

env:
  MODE: "json-rpc"
